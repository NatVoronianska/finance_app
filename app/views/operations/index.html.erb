<p style="color: green"><%= notice %></p>

<h1>Transactions</h1>
<hr>

<div class="container text-center">
  <div class="row border border-secondary border-bottom-0 bg-secondary bg-gradient py-3 rounded-top">
      <div class="col-md-1">ID</div>
      <div class="col-md-2">amount</div>
      <div class="col-md-2">date</div>
      <div class="col-md-2">description</div>
      <div class="col-md-2">category</div>
      <div class="col-md-3">УПРАВЛІННЯ</div>
  </div>

<%= render partial: "operation", collection: @operations %>
<%= link_to "Create new operation", new_category_operation_path(@category), class: "btn btn-outline-secondary" %>
</div>
